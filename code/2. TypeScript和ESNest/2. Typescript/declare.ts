import FOO, { Directions, Options, foo } from "index"

jQuery("#foo")

let diretions = [Directions.Up, Directions.Down]

let options: Options = {
  data: {
    name: 'foo'
  }
}

console.log("foo.name",foo.name);
foo.bar.baz()

console.log("111",FOO);